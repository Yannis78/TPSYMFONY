    <nav class="navbar navbar-expand-lg navbar-light bg-danger">

        <a class="navbar-brand" href=" {{ path('homepage')}} ">TP Yannis K</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_ads_index') }}"> Gestion des annonces </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('homepage') }}" target="_blank"> Voir le site </a>
                </li>
            </ul>
            <ul>
                <ul class="navbar-nav ml-auto"> 
                        <li class="nav-item">
                            <a href="{{ path('admin_ads_create') }}" class="nav-link">Créer une annonce</a>
                        </li>
                </ul>
            </ul>
        </div>
    </nav>

    {% for label, messages in app.flashes %}
        <div class="container">
            <div class="alert alert-{{ label }}">
                {% for message in messages %}
                    <p>{{ message | raw }}</p>
                {% endfor %}
            </div>
        </div>
    {% endfor %}